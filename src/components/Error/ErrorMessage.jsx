import React from 'react';

const ErrorMessage = () => {
    return (
        <span className="error-container">
            ERROR: Город не сущестует или ошибка в названии!
        </span>
    );
};

export default ErrorMessage;